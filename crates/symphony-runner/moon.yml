# Moon project config for symphony-runner (orchestrator daemon binary).
language: 'rust'
layer: 'application'
tasks:
  check:
    deps: ['rust-symphony:format']
    description: 'Run cargo check for the crate'
    command: 'cargo'
    args: ['check']
    inputs:
      - 'Cargo.toml'
      - '/Cargo.lock'
      - 'src/**/*'
    options:
      cache: true
  test:
    description: 'Run cargo test for the crate'
    command: 'cargo'
    args: ['test']
    deps: ['check']
    inputs:
      - 'Cargo.toml'
      - '/Cargo.lock'
      - 'src/**/*'
      - 'tests/**/*'
    options:
      cache: true
