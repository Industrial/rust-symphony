# Moon project config for symphony-workflow crate.
language: 'rust'
layer: 'library'
tasks:
  check:
    description: 'Run cargo check for the crate'
    command: 'cargo'
    args: ['check']
    deps: ['rust-symphony:format']
    inputs:
      - 'Cargo.toml'
      - '/Cargo.lock'
      - 'src/**/*'
    options:
      cache: true
      outputStyle: buffer-only-failure
  test:
    description: 'Run cargo nextest for the crate'
    command: 'cargo'
    args: ['nextest', 'run']
    deps: ['check']
    inputs:
      - 'Cargo.toml'
      - '/Cargo.lock'
      - 'src/**/*'
      - 'tests/**/*'
    options:
      cache: true
      outputStyle: buffer-only-failure
